"""
Raw SQL queries for transport demand analytics.

TODO:
- get_monthly_demand_trends(from_date, to_date): monthly volume, revenue, cost per month
- get_demand_by_city(month): demand count and total revenue grouped by origin city
- get_demand_status_breakdown(month): count by status (accepted/rejected/pending)
"""
